# UniversalParrot 配置驱动架构 - Phase 7 总结

> **状态**: 已完成 (Phase 1-5) | **版本**: v1.0 | **日期**: 2026-02-08

---

## 概述

**UniversalParrot** 是一个配置驱动的 AI 代理框架，允许通过 YAML 配置文件创建新的鹦鹉（Parrot），而无需编写代码。它通过统一的执行策略抽象，实现了对现有 Memo、Schedule、Amazing 三只鹦鹉的配置化重构。

### 解决的问题

| 问题 | 解决方案 |
|:-----|:---------|
| **代码重复** - 三只鹦鹉存在大量重复逻辑 | 统一的 `UniversalParrot` 基类 + 执行策略模式 |
| **扩展困难** - 新增鹦鹉需要编写大量代码 | YAML 配置文件 + 工厂模式 |
| **维护成本** - 修改逻辑需要同步多处 | 单点修改，配置即时生效 |
| **策略固化** - 执行模式与代理类型强耦合 | 策略模式，支持 ReAct/Direct/Planning 三种策略 |

### 核心优势

```
传统方式: MemoParrot (代码) + ScheduleParrot (代码) + AmazingParrot (代码)
                ↓ 每个都是独立实现，逻辑重复

配置驱动: UniversalParrot (通用框架) + YAML 配置
                ↓ 一套代码，多种行为
```

---

## 架构设计

### 目录结构

```
ai/agent/universal/
├── universal_parrot.go      # UniversalParrot 核心实现
├── parrot_config.go         # ParrotConfig 配置结构定义
├── parrot_factory.go        # ParrotFactory 配置驱动工厂
├── config_loader.go         # YAML 配置加载器
├── executor.go              # ExecutionStrategy 接口定义
├── react_executor.go        # ReAct 循环执行策略
├── direct_executor.go       # Direct 原生工具调用策略
├── planning_executor.go     # Planning 两阶段规划策略
├── utils.go                 # 工具函数 (streamAnswer 等)
└── parrot_factory_test.go   # 工厂单元测试

config/parrots/
├── memo.yaml                # Memo Parrot 配置
├── schedule.yaml            # Schedule Parrot 配置
└── amazing.yaml             # Amazing Parrot 配置

server/router/api/v1/ai/
└── factory.go               # AgentFactory 集成 ParrotFactory
```

### 核心组件

#### 1. UniversalParrot (通用鹦鹉基类)

```go
type UniversalParrot struct {
    config   *ParrotConfig      // 配置
    strategy ExecutionStrategy  // 执行策略
    llm      ai.LLMService      // LLM 服务
    tools    map[string]agent.ToolWithSchema  // 工具集
    cache    *LRUCache          // LRU 缓存
    stats    *NormalSessionStats // 会话统计
}
```

**关键方法**:
- `ExecuteWithCallback()` - 主执行入口
- `Name()` / `SelfDescribe()` - 元信息
- `GetSessionStats()` - 统计信息

#### 2. ExecutionStrategy (执行策略接口)

```go
type ExecutionStrategy interface {
    Name() string
    Execute(ctx, input, history, tools, llm, callback) (string, *ExecutionStats, error)
    StreamingSupported() bool
}
```

**三种策略**:

| 策略 | 文件 | 描述 | 适用场景 |
|:-----|:-----|:-----|:---------|
| **ReAct** | `react_executor.go` | 循环推理-行动 | 需要多步推理的任务 |
| **Direct** | `direct_executor.go` | 原生 LLM 工具调用 | 现代模型，快速响应 |
| **Planning** | `planning_executor.go` | 两阶段规划 + 执行 | 复杂多工具并发 |

#### 3. ParrotFactory (配置驱动工厂)

```go
type ParrotFactory struct {
    configs       map[string]*ParrotConfig
    configDir     string
    llm           ai.LLMService
    toolFactories map[string]ToolFactoryFunc
}
```

**创建流程**:
```
1. NewParrotFactory() → 加载 YAML 配置
2. CreateParrot(name, userID) → 解析配置
3. CreateParrotFromConfig() → 创建 UniversalParrot
4. 返回 agent.ParrotAgent
```

---

## 配置格式

### YAML 结构

```yaml
# 基本身份
name: memo                    # 唯一标识符
display_name: Memo Parrot     # 显示名称
emoji: "📝"                   # 表情符号

# 执行策略
strategy: react               # react | direct | planning
max_iterations: 10            # 最大迭代次数

# 能力配置
tools:
  - memo_search               # 可用工具列表

# 提示词
system_prompt: |
  You are a helpful assistant...
prompt_hints:
  - "搜索我的笔记"
  - "找一下关于"

# 缓存配置
enable_cache: true
cache_ttl: 5m
cache_size: 100

# 流式支持
enable_streaming: false

# 自描述
self_description:
  title: Memo Parrot
  name: memo
  emoji: "📝"
  capabilities:
    - memo_search
```

### 配置示例

**Memo Parrot** (`config/parrots/memo.yaml`):
```yaml
name: memo
strategy: react
tools:
  - memo_search
system_prompt: |
  You are a helpful assistant for searching and retrieving notes.
  ...
```

**Schedule Parrot** (`config/parrots/schedule.yaml`):
```yaml
name: schedule
strategy: direct
tools:
  - schedule_add
  - schedule_query
  - schedule_update
  - find_free_time
enable_streaming: true
```

**Amazing Parrot** (`config/parrots/amazing.yaml`):
```yaml
name: amazing
strategy: planning
tools:
  - memo_search
  - schedule_add
  - schedule_query
  - schedule_update
  - find_free_time
```

---

## 集成指南

### 1. 创建新鹦鹉

**步骤**: 创建 YAML 文件 → 重启服务

**示例** - 创建一个 `Writer` 鹦鹉:

```yaml
# config/parrots/writer.yaml
name: writer
display_name: Writer Parrot
emoji: "✍️"
strategy: direct
tools:
  - memo_create
  - memo_update
system_prompt: |
  You are a writing assistant. Help users create and edit notes.
  Always be concise and clear.
prompt_hints:
  - "帮我写个笔记"
  - "修改这条笔记"
enable_cache: true
cache_ttl: 10m
```

### 2. 添加新工具

在 `factory.go` 中注册工具工厂:

```go
factories["memo_create"] = func(userID int32) (agentpkg.ToolWithSchema, error) {
    // 工具创建逻辑
}
```

### 3. 启用 UniversalParrot

```go
// server/router/api/v1/ai/factory.go
factory := &AgentFactory{
    useUniversal: true,  // 启用配置驱动模式
    // ...
}
```

### 4. 运行时切换

```go
// 动态启用/禁用
agentFactory.SetUseUniversal(true)

// 检查状态
if agentFactory.UseUniversal() {
    // 使用 UniversalParrot
}
```

---

## 测试验证

### 单元测试

| 测试文件 | 覆盖内容 |
|:---------|:---------|
| `parrot_factory_test.go` | 配置加载、工厂创建 |

**运行测试**:
```bash
go test ./ai/agent/universal/... -v
```

### 集成验证

**验证清单**:
- [x] `memo.yaml` 配置加载
- [x] `schedule.yaml` 配置加载
- [x] `amazing.yaml` 配置加载
- [x] 三种执行策略正常工作
- [x] LRU 缓存命中/未命中
- [x] 会话统计收集
- [x] AgentFactory 集成

### 测试覆盖

```bash
# 配置加载测试
go test -run TestLoadConfigs

# 配置获取测试
go test -run TestGetConfig

# 默认配置测试
go test -run TestDefaultConfigs
```

---

## 执行策略详解

### ReAct 策略

**特点**: 循环推理-行动模式

**流程**:
```
用户输入 → LLM 推理 → 工具调用 → 结果反馈 → LLM 推理 → ...
    ↓                                           ↓
最终答案                                  达到最大迭代
```

**适用**: 复杂多步推理任务

### Direct 策略

**特点**: 原生 LLM 工具调用

**流程**:
```
用户输入 → LLM (ChatWithTools) → 结构化工具调用 → 执行 → 最终答案
```

**适用**: 现代模型，快速响应

### Planning 策略

**特点**: 两阶段规划 + 执行

**流程**:
```
Phase 1 (规划): LLM 决定使用哪些工具
Phase 2 (执行): 并发执行工具
Phase 3 (综合): LLM 合成最终答案
```

**适用**: 复杂多工具并发查询

---

## 数据流图

```
┌─────────────────────────────────────────────────────────────────┐
│                         用户请求                                 │
└────────────────────────────┬────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    AgentFactory.Create()                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  useUniversal?                                          │   │
│  │       Yes ──→ ParrotFactory.CreateMemoParrot()          │   │
│  │       No  ──→ agentpkg.NewMemoParrot() (Legacy)         │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   UniversalParrot.Execute()                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  1. 检查缓存                                           │   │
│  │  2. 构建 messages (history + system_prompt)            │   │
│  │  3. strategy.Execute()                                 │   │
│  │  4. 缓存结果                                           │   │
│  │  5. 累积统计                                           │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   ExecutionStrategy.Execute()                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐ │
│  │   ReAct     │  │   Direct    │  │      Planning           │ │
│  │  循环工具调用│  │  原生调用   │  │  两阶段规划执行         │ │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                        回调事件流                                │
│  thinking → tool_use → tool_result → answer → stats             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 迁移路径

### 阶段 1: 配置文件创建

- [x] 创建 `config/parrots/` 目录
- [x] 编写 `memo.yaml`
- [x] 编写 `schedule.yaml`
- [x] 编写 `amazing.yaml`

### 阶段 2: 核心框架实现

- [x] `UniversalParrot` 基类
- [x] `ParrotFactory` 工厂
- [x] 三种执行策略

### 阶段 3: 集成

- [x] `AgentFactory` 集成
- [x] 工具工厂函数注册
- [x] 特性开关控制

### 阶段 4: 测试

- [x] 单元测试
- [x] 配置加载测试
- [x] 集成验证

---

## 下一步

### Phase 6: 质量审查

- [ ] 代码审查
- [ ] 性能测试
- [ ] 安全审查

### Phase 7: 生产就绪

- [ ] 监控指标完善
- [ ] 错误处理增强
- [ ] 文档补充

### 未来计划

| 优先级 | 任务 | 描述 |
|:-------|:-----|:-----|
| P0 | 流式 UI 优化 | 支持所有策略的流式响应 |
| P1 | 动态工具注册 | 运行时添加新工具 |
| P1 | 配置热更新 | 无需重启服务 |
| P2 | 多语言支持 | i18n 配置 |
| P2 | A/B 测试框架 | 配置版本管理 |

---

## 参考资料

- **设计文档**: `docs/dev-guides/ARCHITECTURE.md`
- **AI 代理**: `ai/agent/`
- **配置文件**: `config/parrots/`
- **测试**: `ai/agent/universal/*_test.go`

---

*文档版本: v1.0 | 最后更新: 2026-02-08*
