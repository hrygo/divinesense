# Orchestrator-Workers 架构

> DivineSense 多代理协作架构

---

## 1. 架构概览

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                  用户输入                                        │
└───────────────────────────────────────┬─────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              FastRouter (快速路由)                               │
│                                                                                 │
│   Cache → Rule → Confidence                                                     │
│                                                                                 │
│   输出: 直接路由 / Orchestrator 编排                                             │
└───────────────────────────────────────┬─────────────────────────────────────────┘
                                        │
                    ┌───────────────────┼───────────────────┐
                    │                   │                   │
                    ▼                   ▼                   ▼
            ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
            │ Memo Expert │     │Sched Expert │     │Orchestrator │
            │   (直接)    │     │   (直接)    │     │   (编排)    │
            └──────┬──────┘     └──────┬──────┘     └──────┬──────┘
                   │                   │                   │
                   │                   │                   ▼
                   │                   │    ┌─────────────────────────────────┐
                   │                   │    │         Orchestrator            │
                   │                   │    │                                 │
                   │                   │    │  Decomposer → Executor → Agg    │
                   │                   │    │        │         │              │
                   │                   │    │        ▼         ▼              │
                   │                   │    │  TaskPlan   HandoffHandler      │
                   │                   │    │                    │            │
                   │                   │    │                    ▼            │
                   │                   │    │              CapabilityMap      │
                   │                   │    └────────────────────┼────────────┘
                   │                   │                         │
                   └───────────────────┼─────────────────────────┘
                                       │
                                       ▼
                            ┌─────────────────┐
                            │   Expert Pool   │
                            │                 │
                            │  Memo | Sched   │
                            │  (配置驱动)     │
                            └────────┬────────┘
                                     │
                                     ▼
                            ┌─────────────────┐
                            │  Final Response │
                            └─────────────────┘
```

---

## 2. 核心组件

### 2.1 FastRouter

快速意图识别与路由。

| 组件 | 功能 | 延迟 |
|:-----|:-----|:-----|
| Cache | 缓存路由决策 | ~0ms |
| Rule | 规则匹配 + 历史分析 | ~0ms |
| Confidence | 置信度评估 | - |

**路由决策**：

| 置信度 | 输出 |
|:-------|:-----|
| ≥ 0.8 | 直接路由到专家 |
| < 0.8 | 交给 Orchestrator 编排 |

### 2.2 Orchestrator

任务编排核心，包含四个子组件：

```
┌─────────────────────────────────────────────────────────────┐
│                       Orchestrator                           │
│                                                             │
│  ┌────────────┐   ┌────────────┐   ┌────────────┐          │
│  │ Decomposer │──→│  Executor  │──→│ Aggregator │          │
│  │ 任务分解   │   │ 任务执行   │   │ 结果聚合   │          │
│  └────────────┘   └─────┬──────┘   └────────────┘          │
│                         │                                   │
│                         ▼                                   │
│                  ┌────────────┐                             │
│                  │  Handoff   │                             │
│                  │  Handler   │                             │
│                  └─────┬──────┘                             │
│                        │                                    │
│                        ▼                                    │
│                 ┌─────────────┐                             │
│                 │CapabilityMap│                             │
│                 └─────────────┘                             │
└─────────────────────────────────────────────────────────────┘
```

#### Decomposer（任务分解器）

```yaml
配置: config/orchestrator/decomposer.yaml

流程:
  1. 理解意图
  2. 匹配专家
  3. 任务分解
  4. 依赖分析

输出: TaskPlan
```

#### Executor（任务执行器）

```go
MaxConcurrency: 3           // 最大并行数
Strategy: goroutine + semaphore

Events:
  - plan        // 任务计划
  - task_start  // 任务开始
  - task_end    // 任务结束
  - handoff     // 任务转交
```

#### Aggregator（结果聚合器）

```yaml
配置: config/orchestrator/aggregator.yaml

触发: plan.aggregate == true && tasks > 1

策略:
  - Primary: LLM 融合
  - Fallback: 简单拼接
```

#### HandoffHandler（任务转交处理器）

处理粘性路由场景下专家能力不足的情况。

**核心洞察**：

```
"能做"有限可枚举 → 专家声明 capabilities
"不能做"无限不可穷尽 → 专家不声明，运行时报告
```

**流程**：

```
专家执行 → 发现超出能力 → 报告 MissingCapability
                              ↓
                    Orchestrator 查询 CapabilityMap
                              ↓
                    Handoff to 目标专家 → 返回结果
```

### 2.3 CapabilityMap

运行时构建的能力到专家映射。

```go
// 从 config/parrots/*.yaml 的 capabilities 聚合
{
    "搜索笔记内容":   ["memo"],
    "按时间浏览笔记": ["memo"],
    "创建日程":       ["schedule"],
    "查询日程安排":   ["schedule"],
}
```

### 2.4 Expert Pool

配置驱动的专家池。

| 专家 | 符号 | 能力 |
|:-----|:-----|:-----|
| Memo | 📝 | 搜索笔记、浏览笔记 |
| Schedule | 📅 | 创建/查询/修改日程 |

**专家配置原则**：

| 声明 | 不声明 |
|:-----|:-------|
| `capabilities`（能做什么） | `limitations`（不能做什么 - 无法穷尽） |
| | `handoff_triggers`（涉及"不能"） |

---

## 3. 数据流

### 3.1 正常流程

```
用户输入
    │
    ▼
FastRouter ──────────────────────────────┐
    │                                     │
    ├─[高置信度]→ 直接路由 → Expert → 响应  │
    │                                     │
    └─[低置信度]                          │
              │                           │
              ▼                           │
        Decomposer                        │
              │                           │
              ▼                           │
         TaskPlan                         │
              │                           │
              ▼                           │
          Executor                        │
              │                           │
              ▼                           │
        Expert Pool                       │
              │                           │
              ▼                           │
         Aggregator ──────────────────────┘
              │
              ▼
          响应
```

### 3.2 Handoff 流程

**场景**：粘性路由后的上下文切换

```
对话轮次 1:
用户: "帮我找一下 PostgreSQL 优化的笔记"
    │
    ▼
FastRouter → Orchestrator → Memo Expert → 成功

对话轮次 2（粘性路由）:
用户: "找到了，顺便帮我安排个会讨论一下"
    │
    ▼
Memo Expert（粘性路由保持上下文）
    │
    └→ "安排会议" 超出能力
              │
              ▼
       报告 MissingCapability: "创建日程"
              │
              ▼
       HandoffHandler
              │
              ├→ CapabilityMap: "创建日程" → [schedule]
              │
              └→ Handoff to Schedule Expert
                        │
                        ▼
                 Schedule Expert → 成功 → 响应
```

---

## 4. 配置结构

```
config/
├── orchestrator/
│   ├── decomposer.yaml     # 任务分解提示词
│   └── aggregator.yaml     # 结果聚合提示词
│
└── parrots/
    ├── memo.yaml           # Memo Expert 配置
    └── schedule.yaml       # Schedule Expert 配置
```

### 专家配置示例

```yaml
# config/parrots/memo.yaml
name: memo
display_name: Memo Parrot
emoji: "📝"

strategy: react
max_iterations: 10
tools:
  - memo_search

system_prompt: |
  你是笔记搜索专家...

# 专家只声明能力，不声明"不能"
self_description:
  name: memo
  title: "笔记搜索专家"
  capabilities:
    - "搜索笔记内容"
    - "按时间浏览笔记"
    - "查找特定话题的记录"
```

---

## 5. 设计决策

### 5.1 两级路由

| 层级 | 组件 | 延迟 | 场景 |
|:-----|:-----|:-----|:-----|
| L1 | FastRouter | ~0ms | 简单意图、高置信度 |
| L2 | Orchestrator | ~400ms | 复杂意图、多意图 |

### 5.2 专家零耦合

```
专家视角:
  - 我能做什么 → 声明 capabilities
  - 我不能做什么 → 不需要知道
  - 谁能做我不能的 → 不需要知道

Orchestrator 视角:
  - 谁能做什么 → CapabilityMap
  - 专家报告"缺失能力" → 查询 → Handoff
```

### 5.3 降级机制

| 场景 | 降级策略 |
|:-----|:---------|
| LLM 分解失败 | Fallback Plan（单专家） |
| LLM 聚合失败 | 简单拼接 |
| 专家不可用 | 返回空计划 |
| 无匹配能力 | 直接回复用户 |

---

## 6. 扩展性

### 新增专家

```
1. 创建 config/parrots/new_expert.yaml
2. 声明 capabilities
3. 完成

无需修改:
  - 其他专家配置
  - Orchestrator 代码
  - CapabilityMap 构建
```

---

## 7. 文件索引

| 组件 | 文件 |
|:-----|:-----|
| Orchestrator | `ai/agents/orchestrator/orchestrator.go` |
| Decomposer | `ai/agents/orchestrator/decomposer.go` |
| Executor | `ai/agents/orchestrator/executor.go` |
| Aggregator | `ai/agents/orchestrator/aggregator.go` |
| HandoffHandler | `ai/agents/orchestrator/handoff.go` |
| CapabilityMap | `ai/agents/orchestrator/capability_map.go` |
| ExpertRegistry | `ai/agents/orchestrator/expert_registry.go` |
| UniversalParrot | `ai/agents/universal/universal_parrot.go` |

---

*更新日期: 2026-02-14*
