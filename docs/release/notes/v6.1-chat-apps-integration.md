# DivineSense v6.1 - Chat Apps Integration Release Notes

> **Release Date**: 2026-02-03
> **Issue**: [#53](https://github.com/hrygo/divinesense/issues/53)
> **Pull Request**: [#64](https://github.com/hrygo/divinesense/pull/64)

---

## 概述

DivineSense v6.1 引入**聊天应用集成**功能，允许用户通过 Telegram、WhatsApp、钉钉等常用聊天平台直接与 DivineSense AI 助手交互。这是继 Geek Mode 和 Evolution Mode 之后， DivineSense AI 代理能力的又一次重要扩展。

---

## 新增功能

### 🤖 多平台聊天应用接入

| 平台 | 类型 | 支持状态 |
|:-----|:-----|:---------|
| **Telegram** | Bot API | ✅ 完全支持 |
| **WhatsApp** | Baileys 桥接 | ✅ 完全支持 |
| **钉钉** | 企业机器人 | ✅ 完全支持 |

### 🔐 安全加固

- **AES-256-GCM 加密**：所有访问令牌使用 AES-256-GCM 加密存储
- **启动时验证**：服务启动时验证加密密钥配置，失败快速报错
- **输入验证**：平台白名单 + 字段长度限制（防注入）
- **Webhook 签名验证**：
  - 钉钉：HMAC-SHA256 + 时间戳验证（5分钟窗口，防重放攻击）
  - Telegram：Bot Token 匹配验证
  - WhatsApp：桥接服务连接状态检查
- **并发安全**：修复 Token 缓存竞态条件

### 📝 前端管理界面

- 设置页面新增「聊天应用」配置面板
- 支持凭证注册、删除、启用/禁用
- Webhook URL 自动生成
- 完整的 i18n 支持（中英文）

### 🤖 AI 代理集成

- 消息自动路由到合适的 AI 代理（灰灰/时巧/折衷）
- 支持流式响应，提升用户体验
- 支持笔记查询、日程管理等核心功能

---

## 技术变更

### 新增数据库表

```sql
CREATE TABLE chat_app_credential (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES "user"(id) ON DELETE CASCADE,
  platform TEXT NOT NULL,
  platform_user_id TEXT NOT NULL,
  platform_chat_id TEXT,
  access_token TEXT,
  app_secret TEXT,
  webhook_url TEXT,
  enabled BOOLEAN DEFAULT true,
  created_ts BIGINT NOT NULL,
  updated_ts BIGINT NOT NULL,
  UNIQUE(user_id, platform)
);
```

### 新增 API 服务

| RPC 方法 | 描述 |
|:---------|:-----|
| `RegisterCredential` | 注册聊天应用凭证 |
| `ListCredentials` | 列出用户的凭证 |
| `DeleteCredential` | 删除凭证 |
| `HandleWebhook` | 处理平台 Webhook |
| `SendMessage` | 发送消息到聊天平台 |
| `GetWebhookInfo` | 获取 Webhook 配置信息 |

### 新增目录

```
plugin/chat_apps/
├── channels/
│   ├── base.go           # ChatChannel 接口
│   ├── router.go         # 通道路由器
│   ├── telegram/         # Telegram Bot 实现
│   ├── whatsapp/         # WhatsApp 桥接
│   └── dingtalk/         # 钉钉机器人
├── store/
│   ├── db.go              # 数据库操作
│   └── crypto.go          # Token 加密
├── metrics/
│   └── metrics.go         # 指标收集
└── types.go               # 类型定义
```

### 环境变量

| 变量 | 必须 | 说明 |
|:-----|:----:|:-----|
| `DIVINESENSE_CHAT_APPS_SECRET_KEY` | ✅ | 32字节加密密钥 |
| `DIVINESENSE_INSTANCE_URL` | ✅ | 实例 URL（生成 Webhook） |
| `DIVINESENSE_WHATSAPP_BRIDGE_URL` | ❌ | WhatsApp 桥接服务地址 |

---

## 升级指南

### 从 v6.0 升级

1. **备份现有数据**：
   ```bash
   make db-backup
   ```

2. **拉取最新代码**：
   ```bash
   git fetch origin
   git checkout v6.1
   ```

3. **运行数据库迁移**：
   ```bash
   make db-upgrade
   ```

4. **配置环境变量**：
   ```bash
   # 生成 32 字节加密密钥
   openssl rand -base64 32 | head -c 32 > .chat_apps_secret_key

   # 设置环境变量
   export DIVINESENSE_CHAT_APPS_SECRET_KEY=$(cat .chat_apps_secret_key)
   export DIVINESENSE_INSTANCE_URL=https://your-domain.com
   ```

5. **重新构建并启动**：
   ```bash
   make build-all
   make start
   ```

### Docker 部署

```bash
docker pull hrygo/divinesense:v6.1
docker stop divinesense
docker rm divinesense

docker run -d --name divinesense \
  -p 5230:5230 \
  -v ~/.divinesense:/var/opt/divinesense \
  -e DIVINESENSE_CHAT_APPS_SECRET_KEY=<your-key> \
  -e DIVINESENSE_INSTANCE_URL=https://your-domain.com \
  hrygo/divinesense:v6.1
```

---

## 配置指南

### Telegram Bot 配置

1. 与 [@BotFather](https://t.me/BotFather) 对话，创建新 Bot
2. 获取 Bot Token
3. 在 DivineSense 设置页面添加 Telegram 账号
4. 设置 Webhook

**详细指南**：[Telegram 配置指南](../user-guides/CHAT_APPS.md#telegram-配置指南)

### 钉钉机器人配置

1. 登录 [钉钉开放平台](https://open.dingtalk.com/)
2. 创建企业内部应用，获取 AppKey 和 AppSecret
3. 配置消息推送地址
4. 在 DivineSense 设置页面添加钉钉账号

**详细指南**：[钉钉配置指南](../user-guides/CHAT_APPS.md#钉钉配置指南)

### WhatsApp 配置（可选）

WhatsApp 需要运行额外的 Node.js 桥接服务：

```bash
cd plugin/chat_apps/channels/whatsapp/bridge
npm install
npm start
```

**详细指南**：[WhatsApp 配置指南](../user-guides/CHAT_APPS.md#whatsapp-配置指南)

---

## 已知问题

| 问题 | 影响 | 解决方案 |
|:-----|:----:|:---------|
| 钉钉媒体上传 | 不支持图片/文件自动发送 | 以文本形式提示 |
| WhatsApp 桥接服务 | 需要独立部署 | 使用 PM2 或 systemd 管理 |
| SQLite 数据库 | 不支持 Chat Apps 功能 | 使用 PostgreSQL |

---

## 文档更新

- [x] [用户指南](../user-guides/CHAT_APPS.md)
- [x] [开发者指南](../guides/CHAT_APPS.md)
- [x] [架构文档](../dev-guides/ARCHITECTURE.md)
- [x] [后端开发指南](../dev-guides/BACKEND_DB.md)
- [x] [前端开发指南](../dev-guides/FRONTEND.md)
- [x] [技术规格](../specs/chat-apps-integration.md)

---

## 贡献者

- [@hrygo](https://github.com/hrygo) - 架构设计与实现
- [Claude Opus 4.5](https://github.com/anthropics) - 代码审查与安全加固

---

## 许可证

MIT License - 详见 [LICENSE](../../LICENSE)

---

**下载地址**：
- Docker Hub: `hrygo/divinesense:v6.1`
- GitHub Release: [v6.1](https://github.com/hrygo/divinesense/releases/tag/v6.1)
