# Baileys Bridge 服务配置
# 复制此文件为 .env 并修改相应值

# HTTP 服务端口 (默认: 3001)
PORT=3001

# DivineSense Webhook URL
# 注意: 必须使用 DivineSense 服务器的实际 IP 或域名，不能用 localhost
# 开发环境 (同服务器): http://localhost:5230/api/v1/chat_apps/webhook
# 生产环境 (替换 IP): http://YOUR_SERVER_IP:5230/api/v1/chat_apps/webhook
# 生产环境 (域名): https://your-domain.com/api/v1/chat_apps/webhook
DIVINESENSE_WEBHOOK_URL=http://localhost:5230/api/v1/chat_apps/webhook

# API 密钥 (可选，生产环境推荐配置)
# 设置后，/send 和 /download 接口需要在请求头中携带 X-Bridge-Api-Key
# BRIDGE_API_KEY=your-secret-key-here

# CORS 允许的源 (可选，默认允许所有)
# 生产环境建议限制为 DivineSense 的域名，多个源用逗号分隔
# ALLOWED_ORIGINS=http://localhost:5230,https://your-domain.com

# Baileys 认证文件路径 (相对于 bridge 目录)
BAILEYS_AUTH_FILE=./baileys_auth_info.json

# Node 环境 (默认: production)
NODE_ENV=production

# 日志级别 (可选: debug, info, warn, error)
LOG_LEVEL=info

# 心跳检查间隔 (秒，可选)
HEARTBEAT_INTERVAL=30
